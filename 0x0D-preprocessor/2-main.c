#include <stdio.h>

/**
 * main - prints the name of the file of the
 * program
 * was complied from, folllowed by new line
 * Return: always return 0
 */

int main(void)
{
	printf("Files %s\n", __FILE__);

	return (0);
}
